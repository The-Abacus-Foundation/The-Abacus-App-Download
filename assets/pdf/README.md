# Abacus PDF desktop installers

This folder is reserved for **Abacus PDF** installers and release metadata.

The PDF workflow syncs these files here under two locations:

- `assets/pdf/latest/` for stable download links
- `assets/pdf/v{version}/` for versioned archives

These files are separate from the accounting download assets in `assets/v{version}/`.
